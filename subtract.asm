DATA SEGMENT
NUM1 DW 2222H
NUM2 DW 1111H
RES DW ?
BORROW DW 0000H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:
    MOV AX,DATA
    MOV DS,AX

    MOV AX,NUM1
    MOV BX,NUM2
    SUB AX,BX

    MOV RES, AX
    JNC NEXT
        MOV BORROW,0001H
    
    NEXT:
        MOV AH,4CH
        INT 21H

END CODE
END START
